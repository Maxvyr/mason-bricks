# Register bricks which can be consumed via the Mason CLI.
# https://github.com/felangel/mason
bricks:
  # Sample Brick
  # Run `mason make hello` to try it out.
  theme_package:
    path: ./bricks/theme_package
  package:
    path: ./bricks/package
  riverpod_module:
    path: ./bricks/riverpod_module
