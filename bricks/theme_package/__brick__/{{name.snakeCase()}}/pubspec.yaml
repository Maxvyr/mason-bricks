name: {{name.snakeCase()}}
description: theme of the app
version: 0.0.1
homepage:

environment:
  sdk: ">=2.19.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  shared_preferences:

dev_dependencies:
  flutter_lints:
